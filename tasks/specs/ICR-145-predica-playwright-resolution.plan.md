# ICR-145 — `/predica` playwright resolution: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the three root-invoked `/predica` harness scripts resolve `@playwright/test` on a clean checkout, delete the symlink workaround, and add a CI smoke check that would have caught the regression.

**Architecture:** The bug is a dependency-_placement_ problem, not a code problem. `@playwright/test` lives only in `apps/web/node_modules`; Node's bare-specifier walk starts at the importing module's directory and stops at `<repoRoot>/node_modules`. Adding `@playwright/test` to the **root** `devDependencies` makes the walk succeed and fixes all three scripts with **zero script edits**. A new `__smoke__.mjs` runner (invoked by `pnpm predica:smoke`, wired into a new CI job) invokes two of the scripts end-to-end against the committed fixture and asserts positively on exit code + non-empty outputs.

**Tech Stack:** pnpm 10 workspace + Turborepo · Node 22.x (ESM `.mjs`) · Playwright 1.61.0 (chromium) · GitHub Actions

**Spec:** `tasks/specs/ICR-145-predica-playwright-resolution.md` — read it first.

## Global Constraints

- **Package manager is `pnpm`.** Never `npm`/`yarn`. Root scripts proxy through Turbo.
- **Playwright version is exactly `1.61.0`** — pin without a caret at the root. `apps/web` already resolves 1.61.0; one version ⇒ one browser revision.
- **Do NOT edit the three `build-predica-*.mjs` scripts.** Their `import { chromium } from "@playwright/test";` is already correct. No `createRequire` boilerplate.
- **The featured script must run with `--no-ai` in any automated check** — hermetic, no network, no `GEMINI_API_KEY`, deterministic typographic card.
- **Assert positively.** Every check must prove it _observed_ an artifact (exists AND non-empty) before passing. A check that takes the pass branch when the tool never ran is worse than no check (ICR-144 lesson).
- **Never write into the repo working tree from a check.** Outputs go to `mkdtemp()`. `git status` must be clean after `pnpm predica:smoke`.
- **Valid PDF block types:** `h2`, `h3`, `p`, `blockquote` (use `text`) and `ul`, `ol` (use `items`). **Do NOT use `embeddedAsset`** — validated but never rendered.
- **Conventional Commits**, header ≤ 100 chars, scoped `(ICR-145)`.
- **Verification trap:** worktrees nest inside the main checkout, so Node escapes upward into `<mainRepo>/node_modules` and can find the old workaround symlink. Always assert the _resolved path_ lies inside the worktree's own `node_modules`.

---

## File Structure

| File                                                                                                | Responsibility                                                                                                                                                                  |
| --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `package.json` (root)                                                                               | Declares `@playwright/test@1.61.0` as a root devDep (the fix) and the `predica:smoke` entry point.                                                                              |
| `pnpm-lock.yaml`                                                                                    | Regenerated by `pnpm install`. Never hand-edit.                                                                                                                                 |
| `.claude/scripts/predica/__fixtures__/sample-sermon.json`                                           | The single shared fixture. Must satisfy **both** scripts' validators.                                                                                                           |
| `.claude/scripts/predica/__smoke__.mjs`                                                             | **New.** The regression runner: spawn each script against the fixture into a temp dir, assert exit 0 + non-empty outputs, clean up. Sole responsibility: prove the scripts run. |
| `.github/workflows/pr.yml`                                                                          | **New job** `predica-scripts` — the only environment (fresh clone, no parent `node_modules`) that honestly proves AC1/AC3.                                                      |
| `.claude/commands/predica.md`, `docs/architecture/predica-featured-image.md`, `.claude/config.json` | Browser-provisioning guidance → root form.                                                                                                                                      |

---

## Task 1: Root dependency + remove the workaround symlink

**Files:**

- Modify: `package.json` (root, `devDependencies`)
- Modify: `pnpm-lock.yaml` (regenerated — do not hand-edit)

**Interfaces:**

- Consumes: nothing.
- Produces: a resolvable `@playwright/test` (v1.61.0) at `<repoRoot>/node_modules/@playwright/test`, plus the `playwright` binary at `<repoRoot>/node_modules/.bin/playwright`. Every later task depends on this.

- [ ] **Step 1: Prove the bug, uncontaminated by the parent checkout**

The worktree nests inside the main checkout, so a naive run resolves through the parent's workaround symlink. Delete that symlink **first**, then confirm the failure is real:

```bash
rm -rf /Users/gabriel/repos/idc-redentor-website/node_modules/@playwright
cd /Users/gabriel/repos/idc-redentor-website/.claude/worktrees/ICR-145
node .claude/scripts/predica/build-predica-featured.mjs \
  .claude/scripts/predica/__fixtures__/sample-sermon.json --no-ai --out /tmp/icr145-before
```

Expected: **FAIL** with `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@playwright/test'`.

> If it instead succeeds, the symlink was not actually removed (or another parent `node_modules` supplies it). Stop and re-check — do not proceed on a false negative.

- [ ] **Step 2: Add the root devDependency**

Edit root `package.json`. Insert `@playwright/test` into `devDependencies`, keeping the keys alphabetically sorted (it sorts first, before `@commitlint/cli`):

```json
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
```

Exact pin, **no caret** — see Global Constraints.

- [ ] **Step 3: Install**

```bash
cd /Users/gabriel/repos/idc-redentor-website/.claude/worktrees/ICR-145
pnpm install
```

Expected: `pnpm-lock.yaml` updates; `+ @playwright/test 1.61.0` appears in the root importer.

- [ ] **Step 4: Verify resolution lands INSIDE the worktree (not the parent)**

```bash
node -p "require('node:module').createRequire('$PWD/.claude/scripts/predica/build-predica-pdf.mjs').resolve('@playwright/test')"
```

Expected: a path beginning with `.../\.claude/worktrees/ICR-145/node_modules/` — **not** `/Users/gabriel/repos/idc-redentor-website/node_modules/`. This is the assertion that defeats the nesting trap.

- [ ] **Step 5: Verify the import now resolves (the PDF script gets past the import)**

```bash
node .claude/scripts/predica/build-predica-pdf.mjs \
  .claude/scripts/predica/__fixtures__/sample-sermon.json --out /tmp/icr145-after
```

Expected: **no** `ERR_MODULE_NOT_FOUND`. It should now fail _only_ on fixture validation:

```
error: invalid sermon.json:
  - locales.es-AR.content: required non-empty array of blocks
  - locales.en-US.content: required non-empty array of blocks
```

That error is **the proof the import resolved** — Task 2 fixes the fixture.

And confirm the featured script (which does not need `content`) now fully passes:

```bash
node .claude/scripts/predica/build-predica-featured.mjs \
  .claude/scripts/predica/__fixtures__/sample-sermon.json --no-ai --out /tmp/icr145-after
```

Expected: exit 0, `featured.png` written, and the log line `note: GEMINI_API_KEY not set — using the typographic fallback card` (or the `--no-ai` path) — i.e. no network call.

- [ ] **Step 6: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "fix(ICR-145): add @playwright/test to root devDependencies"
```

---

## Task 2: Make the fixture valid for the PDF script

**Files:**

- Modify: `.claude/scripts/predica/__fixtures__/sample-sermon.json`

**Interfaces:**

- Consumes: the resolvable playwright from Task 1.
- Produces: a fixture that satisfies **both** validators — `build-predica-pdf.mjs`'s (needs `locales.<loc>.content[]` for **both** `es-AR` and `en-US`) and `build-predica-featured.mjs`'s (needs only `slug`/`sermonDate`/`preacher`/`locales["es-AR"].title`). Task 3's smoke runner depends on this.

**Context:** the fixture today has **no `content` field at all**. The addition is purely additive — every existing field stays untouched. The content must be faithful to the fixture's existing Ephesians 2 sermon ("El amor que derriba muros") and must exercise **all six renderable block types** (`h2`, `h3`, `p`, `blockquote`, `ul`, `ol`) so the smoke check doubles as a renderer regression net.

- [ ] **Step 1: Add `content` to `locales["es-AR"]`**

Insert a `content` key into the `es-AR` locale object (alongside `title`, `lead`, … — order within the object does not matter):

```json
      "content": [
        {
          "type": "p",
          "text": "Vivimos en un mundo marcado por la división: barreras nacionales, culturales, sociales y personales que nos separan unos de otros. Sin embargo, el evangelio proclama que en Cristo hay una reconciliación radical que ninguna frontera humana puede contener."
        },
        { "type": "h2", "text": "1. La realidad de la división humana" },
        {
          "type": "p",
          "text": "Pablo comienza recordándoles a los efesios quiénes eran antes de Cristo. No suaviza el diagnóstico: la distancia era real y no se cerraba con esfuerzo humano (Efesios 2:11-12)."
        },
        {
          "type": "ul",
          "items": [
            "Separados de Cristo.",
            "Ajenos a la ciudadanía de Israel.",
            "Sin esperanza y sin Dios en el mundo."
          ]
        },
        { "type": "h2", "text": "2. La obra reconciliadora de Cristo" },
        {
          "type": "blockquote",
          "text": "«Porque él es nuestra paz, que de ambos pueblos hizo uno, derribando la pared intermedia de separación.» — Efesios 2:14 (RVR1960)"
        },
        {
          "type": "p",
          "text": "El muro no fue escalado ni rodeado: fue derribado. En la cruz, Cristo abolió la enemistad y creó en sí mismo una nueva humanidad."
        },
        { "type": "h3", "text": "Tres verbos de la cruz" },
        {
          "type": "ol",
          "items": [
            "Acercó a los que estaban lejos (v. 13).",
            "Derribó la pared intermedia de separación (v. 14).",
            "Reconcilió a ambos con Dios en un solo cuerpo (v. 16)."
          ]
        },
        { "type": "h2", "text": "3. La nueva humanidad en Cristo" },
        {
          "type": "blockquote",
          "text": "«Ya no sois extranjeros ni advenedizos, sino conciudadanos de los santos, y miembros de la familia de Dios.» — Efesios 2:19 (RVR1960)"
        },
        {
          "type": "p",
          "text": "Que este mensaje nos desafíe a vivir la paz que Cristo ya compró: a cruzar fronteras, a acoger al diferente, y a construir la nueva humanidad que Dios está edificando en medio de nosotros."
        }
      ]
```

- [ ] **Step 2: Add `content` to `locales["en-US"]`**

The PDF script validates **both** locales — an `es-AR`-only fixture still fails. Mirror the same structure:

```json
      "content": [
        {
          "type": "p",
          "text": "We live in a world marked by division: national, cultural, social, and personal barriers that keep us apart. Yet the gospel proclaims that in Christ there is a radical reconciliation that no human boundary can contain."
        },
        { "type": "h2", "text": "1. The reality of human division" },
        {
          "type": "p",
          "text": "Paul begins by reminding the Ephesians who they were before Christ. He does not soften the diagnosis: the distance was real, and no human effort could close it (Ephesians 2:11-12)."
        },
        {
          "type": "ul",
          "items": [
            "Separated from Christ.",
            "Excluded from citizenship in Israel.",
            "Without hope and without God in the world."
          ]
        },
        { "type": "h2", "text": "2. The reconciling work of Christ" },
        {
          "type": "blockquote",
          "text": "\"For he himself is our peace, who has made the two groups one and has destroyed the barrier, the dividing wall of hostility.\" — Ephesians 2:14 (NIV)"
        },
        {
          "type": "p",
          "text": "The wall was not climbed or circled around: it was torn down. On the cross, Christ abolished the hostility and created in himself one new humanity."
        },
        { "type": "h3", "text": "Three verbs of the cross" },
        {
          "type": "ol",
          "items": [
            "He brought near those who were far off (v. 13).",
            "He destroyed the dividing wall of hostility (v. 14).",
            "He reconciled both to God in one body (v. 16)."
          ]
        },
        { "type": "h2", "text": "3. The new humanity in Christ" },
        {
          "type": "blockquote",
          "text": "\"Consequently, you are no longer foreigners and strangers, but fellow citizens with God's people and also members of his household.\" — Ephesians 2:19 (NIV)"
        },
        {
          "type": "p",
          "text": "May this message challenge us to live out the peace Christ has already purchased: to cross boundaries, to welcome the stranger, and to build the new humanity that God is raising up among us."
        }
      ]
```

- [ ] **Step 3: Verify the JSON is well-formed**

```bash
node -e "const s=require('./.claude/scripts/predica/__fixtures__/sample-sermon.json');
for (const l of ['es-AR','en-US']) {
  const c = s.locales[l].content;
  console.log(l, 'blocks:', c.length, 'types:', [...new Set(c.map(b=>b.type))].join(','));
}"
```

Expected: each locale reports `blocks: 12` and `types: p,h2,ul,blockquote,h3,ol` (all six renderable types present).

- [ ] **Step 4: Verify BOTH scripts now run green against the fixture**

```bash
rm -rf /tmp/icr145-t2 && mkdir -p /tmp/icr145-t2
node .claude/scripts/predica/build-predica-pdf.mjs \
  .claude/scripts/predica/__fixtures__/sample-sermon.json --out /tmp/icr145-t2
echo "pdf exit=$?"
node .claude/scripts/predica/build-predica-featured.mjs \
  .claude/scripts/predica/__fixtures__/sample-sermon.json --no-ai --out /tmp/icr145-t2
echo "featured exit=$?"
ls -l /tmp/icr145-t2
```

Expected: both `exit=0`; `/tmp/icr145-t2` contains `predica.es-AR.pdf`, `predica.en-US.pdf`, `featured.png`, all non-trivial in size (PDFs > 20 KB typically).

- [ ] **Step 5: Confirm the working tree is clean of stray outputs**

```bash
git status --short
```

Expected: only `.claude/scripts/predica/__fixtures__/sample-sermon.json` is modified. **No** PDFs or PNGs inside the repo (the `--out` flag kept them in `/tmp`).

- [ ] **Step 6: Commit**

```bash
git add .claude/scripts/predica/__fixtures__/sample-sermon.json
git commit -m "test(ICR-145): add content blocks to the predica sample-sermon fixture"
```

---

## Task 3: The smoke runner + `pnpm predica:smoke`

**Files:**

- Create: `.claude/scripts/predica/__smoke__.mjs`
- Modify: `package.json` (root, `scripts`)

**Interfaces:**

- Consumes: the root playwright dep (Task 1) and the valid fixture (Task 2).
- Produces: `pnpm predica:smoke` — exits **0** on success, **1** on any failure. Task 4's CI job calls exactly this.

- [ ] **Step 1: Write the smoke runner**

Create `.claude/scripts/predica/__smoke__.mjs`:

```js
#!/usr/bin/env node
/**
 * Regression smoke check for the root-invoked /predica harness scripts (ICR-145).
 *
 * Runs the PDF + featured-image scripts against the committed fixture and asserts
 * each one exits 0 AND writes a non-empty output. This guards the bare-specifier
 * resolution of "@playwright/test" from the repo root: before ICR-145 these scripts
 * died with ERR_MODULE_NOT_FOUND, because @playwright/test was installed only into
 * apps/web/node_modules and Node's resolution walk never reaches it.
 *
 * Hermetic by construction: the featured script runs with --no-ai, so there is no
 * network call and no GEMINI_API_KEY is required.
 *
 * Usage: pnpm predica:smoke
 */
import { spawnSync } from "node:child_process";
import { existsSync, mkdtempSync, rmSync, statSync } from "node:fs";
import { tmpdir } from "node:os";
import path from "node:path";
import { fileURLToPath } from "node:url";

const HERE = path.dirname(fileURLToPath(import.meta.url));
const FIXTURE = path.join(HERE, "__fixtures__", "sample-sermon.json");

/** Outputs below this are treated as truncated/empty rather than a real render. */
const MIN_BYTES = 1024;

const CASES = [
  {
    script: "build-predica-pdf.mjs",
    args: [],
    outputs: ["predica.es-AR.pdf", "predica.en-US.pdf"],
  },
  {
    script: "build-predica-featured.mjs",
    args: ["--no-ai"],
    outputs: ["featured.png"],
  },
];

let failures = 0;

/** @param {string} msg */
function fail(msg) {
  process.stderr.write(`✗ ${msg}\n`);
  failures += 1;
}

const outDir = mkdtempSync(path.join(tmpdir(), "predica-smoke-"));

try {
  if (!existsSync(FIXTURE)) {
    fail(`fixture not found: ${FIXTURE}`);
  } else {
    for (const { script, args, outputs } of CASES) {
      const scriptPath = path.join(HERE, script);
      const res = spawnSync(
        process.execPath,
        [scriptPath, FIXTURE, ...args, "--out", outDir],
        { encoding: "utf8" },
      );

      if (res.error) {
        fail(`${script}: could not spawn — ${res.error.message}`);
        continue;
      }
      if (res.status !== 0) {
        fail(
          `${script}: exited ${res.status}\n` +
            `--- stdout ---\n${res.stdout}\n--- stderr ---\n${res.stderr}`,
        );
        continue;
      }

      // Assert positively: the artifact must exist AND be non-empty. A check that
      // passes when nothing ran is worse than no check at all.
      for (const name of outputs) {
        const outPath = path.join(outDir, name);
        if (!existsSync(outPath)) {
          fail(`${script}: expected output missing: ${name}`);
          continue;
        }
        const { size } = statSync(outPath);
        if (size < MIN_BYTES) {
          fail(
            `${script}: ${name} is only ${size} bytes (expected >= ${MIN_BYTES})`,
          );
          continue;
        }
        process.stdout.write(`✓ ${script} → ${name} (${size} bytes)\n`);
      }
    }
  }
} finally {
  rmSync(outDir, { recursive: true, force: true });
}

if (failures > 0) {
  process.stderr.write(`\n✗ predica smoke FAILED (${failures} problem(s))\n`);
  process.exit(1);
}

process.stdout.write(
  "\n✓ predica smoke passed — scripts resolved @playwright/test and rendered\n",
);
```

- [ ] **Step 2: Add the root script**

In root `package.json`, add `predica:smoke` to `scripts` (after `e2e`, before `format`):

```json
    "e2e": "turbo run e2e",
    "predica:smoke": "node .claude/scripts/predica/__smoke__.mjs",
    "format": "prettier --write .",
```

> Note: this is a **direct** `node` invocation, not a Turbo proxy — the predica scripts are root harness tooling and are not a workspace package target.

- [ ] **Step 3: Run it — expect PASS**

```bash
pnpm predica:smoke
echo "exit=$?"
```

Expected: `exit=0` and three `✓` lines (`predica.es-AR.pdf`, `predica.en-US.pdf`, `featured.png`), then `✓ predica smoke passed`.

- [ ] **Step 4: Prove the check can actually FAIL**

A check that cannot fail is not a check. Temporarily break the fixture and confirm a non-zero exit:

```bash
cp .claude/scripts/predica/__fixtures__/sample-sermon.json /tmp/icr145-fixture.bak
node -e "
const fs=require('fs'); const p='.claude/scripts/predica/__fixtures__/sample-sermon.json';
const s=JSON.parse(fs.readFileSync(p,'utf8'));
delete s.locales['es-AR'].content;             // re-introduce the original bug
fs.writeFileSync(p, JSON.stringify(s,null,2));
"
pnpm predica:smoke; echo "exit=$? (MUST be 1)"

# restore
cp /tmp/icr145-fixture.bak .claude/scripts/predica/__fixtures__/sample-sermon.json
git diff --stat .claude/scripts/predica/__fixtures__/sample-sermon.json   # expect: no diff
```

Expected: the broken run prints `✗ build-predica-pdf.mjs: exited 1` with the validator error and exits **1**. After restore, `git diff` on the fixture is empty.

- [ ] **Step 5: Confirm no working-tree pollution**

```bash
pnpm predica:smoke >/dev/null && git status --short
```

Expected: only the two intended files (`.claude/scripts/predica/__smoke__.mjs`, `package.json`) show — the smoke run itself leaves nothing behind (outputs went to `mkdtemp` and were removed).

- [ ] **Step 6: Lint + format**

```bash
pnpm lint && pnpm format:check
```

Expected: both pass. If `format:check` flags `__smoke__.mjs`, run `pnpm format` and re-check.

- [ ] **Step 7: Commit**

```bash
git add .claude/scripts/predica/__smoke__.mjs package.json
git commit -m "test(ICR-145): add predica scripts smoke check"
```

---

## Task 4: CI job

**Files:**

- Modify: `.github/workflows/pr.yml`

**Interfaces:**

- Consumes: `pnpm predica:smoke` (Task 3) and the root `playwright` binary (Task 1).
- Produces: a required-status job named `predica-scripts` on every PR. **This is the authoritative AC1/AC3 proof** — a fresh clone with no parent `node_modules` and no symlink.

- [ ] **Step 1: Add the `predica-scripts` job**

Append to the `jobs:` map in `.github/workflows/pr.yml`, as a sibling of `eslint-tsc` (a **separate job**, so the chromium download runs in parallel with lint/tsc and a failure attributes cleanly):

```yaml
predica-scripts:
  runs-on: ubuntu-latest

  steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "22.x"
        cache: "pnpm"

    - name: Install dependencies
      run: pnpm install --frozen-lockfile

    # The version is read from the root devDependency (exact-pinned), so the browser
    # cache can never pair a stale chromium revision with a bumped playwright.
    - name: Resolve Playwright version
      id: playwright
      run: |
        echo "version=$(node -p "require('./package.json').devDependencies['@playwright/test']")" >> "$GITHUB_OUTPUT"

    - name: Cache Playwright browsers
      uses: actions/cache@v4
      with:
        path: ~/.cache/ms-playwright
        key: ${{ runner.os }}-playwright-${{ steps.playwright.outputs.version }}

    # Run from the ROOT — this only works because @playwright/test is a root devDep,
    # which is precisely the regression this job guards.
    - name: Install Chromium
      run: pnpm exec playwright install --with-deps chromium

    - name: Predica scripts smoke
      run: pnpm predica:smoke
```

- [ ] **Step 2: Verify the YAML parses and the job is well-formed**

```bash
node -e "
const fs=require('fs');
const y=fs.readFileSync('.github/workflows/pr.yml','utf8');
if (!/^  predica-scripts:$/m.test(y)) { console.error('job not found'); process.exit(1); }
console.log('jobs:', [...y.matchAll(/^  ([a-z-]+):$/gm)].map(m=>m[1]).join(', '));
"
```

Expected: `jobs: validate-pr-title, eslint-tsc, predica-scripts`.

If `yamllint` or `actionlint` is available, run it; otherwise the grep above plus the live CI run in Step 4 is the check.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/pr.yml
git commit -m "ci(ICR-145): run the predica scripts smoke check on PRs"
```

- [ ] **Step 4: Verify on the live PR (after the PR is open)**

This step is completed by the orchestrator once the draft PR exists. Expected: the `predica-scripts` job appears on the PR and goes **green** — proving the scripts resolve `@playwright/test` on a clean checkout with **no symlink**, which is AC1 + AC2 + AC3 + AC4 all at once.

---

## Task 5: Docs

**Files:**

- Modify: `.claude/commands/predica.md` (2 occurrences)
- Modify: `docs/architecture/predica-featured-image.md`
- Modify: `.claude/config.json`

**Interfaces:**

- Consumes: the root `playwright` binary (Task 1).
- Produces: nothing consumed downstream.

- [ ] **Step 1: Find every stale provisioning command**

```bash
grep -rn "apps/web exec playwright" .claude/ docs/ || echo "(none)"
```

Expected (before the change): hits in `.claude/commands/predica.md` (×2), `docs/architecture/predica-featured-image.md`, `.claude/config.json`.

- [ ] **Step 2: Replace with the root form**

In each hit, change:

```
pnpm -C apps/web exec playwright install chromium
```

to:

```
pnpm exec playwright install chromium
```

- [ ] **Step 3: Note WHY the dep lives at the root**

In `docs/architecture/predica-featured-image.md`, add a short note near the provisioning command (adapt the surrounding prose/heading style):

```markdown
> **Where the Playwright dependency lives.** `@playwright/test` is a **root** `devDependency`
> (pinned to the same version `apps/web` uses). The `/predica` scripts live at
> `.claude/scripts/predica/`, and Node resolves bare specifiers by walking `node_modules` up
> from the _importing module's own directory_ — a walk that stops at the repo root and never
> reaches `apps/web/node_modules`. A root dep is therefore the only thing that makes
> `import { chromium } from "@playwright/test"` resolve in these scripts (ICR-145). Provision
> the browser from the repo root: `pnpm exec playwright install chromium`.
```

- [ ] **Step 4: Verify nothing stale remains**

```bash
grep -rn "apps/web exec playwright" .claude/ docs/ && echo "STALE REMAINS" || echo "✓ clean"
pnpm format:check
```

Expected: `✓ clean`, and `format:check` passes.

- [ ] **Step 5: Commit**

```bash
git add .claude/commands/predica.md docs/architecture/predica-featured-image.md .claude/config.json
git commit -m "docs(ICR-145): point playwright browser provisioning at the repo root"
```

---

## Final verification (orchestrator)

- [ ] `pnpm type-check && pnpm lint && pnpm test` — the standard stack (should be unaffected; this ticket touches no app source).
- [ ] `pnpm predica:smoke` — exits 0.
- [ ] `git diff --name-only origin/main...HEAD` (**three-dot**, vs the merge-base) — expect exactly: `package.json`, `pnpm-lock.yaml`, `.claude/scripts/predica/__fixtures__/sample-sermon.json`, `.claude/scripts/predica/__smoke__.mjs`, `.github/workflows/pr.yml`, `.claude/commands/predica.md`, `docs/architecture/predica-featured-image.md`, `.claude/config.json`, plus the spec + plan docs.
- [ ] The three `build-predica-*.mjs` scripts are **NOT** in that list.
- [ ] The `predica-scripts` CI job is green on the PR.
- [ ] The main checkout's `node_modules/@playwright` symlink is gone and `pnpm install` has re-created a pnpm-managed path (AC3).

## Spec coverage check

| Spec requirement                                                     | Task                               |
| -------------------------------------------------------------------- | ---------------------------------- |
| R1 root dependency                                                   | 1                                  |
| R2 no script changes                                                 | 1 (asserted in Final verification) |
| R3 valid fixture, all 6 block types, no `embeddedAsset`              | 2                                  |
| R4 smoke runner (positive assertions, temp dir, non-zero on failure) | 3                                  |
| R5 `predica:smoke` root script                                       | 3                                  |
| R6 CI job (+ version-keyed browser cache, root `pnpm exec`)          | 4                                  |
| R7 remove the workaround symlink                                     | 1 (Step 1) + Final verification    |
| R8 docs → root provisioning form                                     | 5                                  |
