query credoCollection(
  $locale: String
  $preview: Boolean
  $limit: Int
  $order: [CredoOrder]
  $where: CredoFilter
) {
  credoCollection(limit: $limit, locale: $locale, preview: $preview, order: $order, where: $where) {
    items {
      ...Credo
    }
  }
}
